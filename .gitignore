# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Data (restricted / large / regenerable — see README for data sources)
/data/raw/
/data/processed/
*.dta
*.osm.pbf
*.gpkg
*.shp
*.shx
*.dbf
*.prj
*.cpg
*.dump

# ML artifacts (regenerable via training pipeline; large binaries)
ml/artifacts/*
*.joblib
*.pkl

# The active production model is small enough to ship as a real repo asset
# rather than something the deploy environment has to regenerate.
!ml/artifacts/lightgbm_opportunity_model.joblib
!ml/artifacts/model_comparison_20260702T190344Z.json

# Notebook checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
