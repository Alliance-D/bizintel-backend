[pytest]
# Only collect the unit test suite. The scripts/ directory contains helper
# utilities (some ending in _test.py) that are run manually, not by pytest.
testpaths = tests
# Put the repo root on sys.path so "import app" works whether tests are run via
# the `pytest` console script or `python -m pytest`.
pythonpath = .
